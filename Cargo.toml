[package]
name = "keepass-merge"
authors = ["louib <code@louib.net>"]

version = "0.1.0"
edition = "2021"

repository = "https://github.com/louib/keepass-merge/"
description = "CLI tool to merge KDBX (keepass) databases"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
#
[[bin]]
name = "keepass-repair"

[dependencies]
clap = { version = "4", features = ["derive"] }
rpassword = { version = "7" }
anyhow = { version = "1" }
regex = "1"
uuid = "1"
chrono = { version = "0.4", features = ["serde"] }
base64 = "0.21"
log = "0.4"
env_logger = "0.10"
ctrlc = "3.4"
atty = "0.2"

[dependencies.keepass]
git = "https://github.com/sseemayer/keepass-rs"
features = ["save_kdbx4", "_merge", "challenge_response"]
